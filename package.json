{
    "name": "arab-contractors-app",
    "version": "1.0.0",
    "description": "Monolithic deployment for Arab Contractors App",
    "scripts": {
        "install-all": "cd client && npm install --production=false && cd ../server && npm install",
        "build": "npm run install-all && cd client && npm run build",
        "start": "node server/index.js",
        "dev": "concurrently \"cd server && node index.js\" \"cd client && npm run dev\""
    },
    "dependencies": {
        "concurrently": "^8.2.2"
    },
    "engines": {
        "node": ">=16.0.0"
    }
}